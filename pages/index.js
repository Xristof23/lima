import Addition from "@/components/Addition";

export default function HomePage({}) {
  return (
    <div>
      <h1>Hello math lover (hater)</h1>
    <Addition upperLimit={20}/>
    <Addition upperLimit={20}/>
    <Addition upperLimit={20}/>
    <Addition upperLimit={20}/>
    <Addition upperLimit={20}/>

    </div>
  );
}
